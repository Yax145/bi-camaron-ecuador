name: shrimp-bi-medallion-ecuador
channels:
  - conda-forge
dependencies:
  - python=3.12.3
  - pip
  - pip:
      - pandas==2.2.3
      - openpyxl==3.1.5
      - requests>=2.31,<3
      - beautifulsoup4>=4.12,<5
      - tenacity>=8.2,<10
# Observed managed Databricks components are documented separately because
# their Databricks Connect build identifiers are not portable PyPI pins.
